#!/bin/bash

# Launch the bluetooth controls TUI (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec hyprland-launch-or-focus-tui bluetui
