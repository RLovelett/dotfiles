#!/bin/bash

# Launch the TUI command passed in as an argument in the default terminal with an me.lovelett.hyprland.COMMAND app id for styling.

exec setsid uwsm-app -- xdg-terminal-exec --app-id=me.lovelett.hyprland.$(basename $1) -e "$1" "${@:2}"
